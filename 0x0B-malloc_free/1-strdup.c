#include "main.h"
#include <stdlib.h>
/**
*_strdup - function returns a newly allocated space
*@str: initial string
*@nstr: new string
*Return: a pointer
*/
char *_strdup(char *str)
{
    char *str;
    char *nstr;

    *nstr = strcpy(str, nstr);
    nstr = char *malloc(unsigned int size, *nstr);
    if (size == 0)
        return (NULL);
    return (*nstr);
}
